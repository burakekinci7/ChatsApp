import 'package:flutter/material.dart';

class IconCustomConst {
  static const IconData chatIcon = Icons.message;
  static const Icon signOutIcon = Icon(Icons.logout);
  static const Icon sendMessage = Icon(Icons.send);
  static const Icon googleSignIn = Icon(Icons.g_mobiledata);
  static const Icon sendPost = Icon(Icons.arrow_circle_up);
  static const Icon profilePicPerson = Icon(Icons.person);
  static const Icon settings = Icon(Icons.settings);
  static const Icon comment = Icon(Icons.comment);
  static const Icon delete = Icon(Icons.delete);
}
